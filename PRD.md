# PRD.md - 轨道交通智能问答助手 产品需求文档

## 一、产品概述

### 1.1 产品定位
面向轨道交通工程设计场景的私有化智能问答助手，通过RAG（检索增强生成）技术，将工程规范文档转化为可语义检索的知识库，帮助工程师快速精准地获取规范参数与设计要求。

### 1.2 目标用户
- **主要用户**：轨道交通工程设计工程师
- **次要用户（本项目展示场景）**：面试官 / 技术评审

### 1.3 核心价值
- **快**：单次规范检索从5分钟降至30秒以内
- **准**：语义理解替代关键词匹配，准确率95%
- **全**：自动关联跨文档/跨章节参数，无需人工翻阅
- **安**：全链路私有化部署，数据不出本地

---

## 二、核心用户流程

### 2.1 主流程：智能问答

```
用户打开Web页面
    ↓
输入工程问题（如："桥梁墩身混凝土强度等级要求？"）
    ↓
系统进行语义检索（向量检索 + 关键词混合检索）
    ↓
召回相关规范片段（Top-K = 8）
    ↓
双层Prompt引导LLM推理生成答案
    ↓
正则校验输出格式，拦截幻觉
    ↓
展示答案 + 规范来源引用（文档名 + 章节号）
    ↓
用户可点击来源查看原文上下文
```

### 2.2 辅助流程：知识库管理（管理端）

```
管理员上传工程规范文档（Word/PDF/TXT）
    ↓
系统自动清洗文档（去页眉页脚、统一编码、表格转文本）
    ↓
语义分块（按章节标题+正文为单元）
    ↓
向量化存储（Embedding → 向量数据库）
    ↓
知识库就绪，前端可检索
```

### 2.3 辅助流程：Prompt策略配置（管理端）

```
管理员进入策略配置页
    ↓
查看/编辑双层Prompt模板
    ↓
调整参数（chunk_size、top_k、温度等）
    ↓
保存配置，实时生效
```

---

## 三、功能详情

### 3.1 MVP阶段核心功能

#### F1：智能问答页面 (P0)

| 属性 | 内容 |
|------|------|
| 功能描述 | 用户输入工程问题，系统返回带来源引用的答案 |
| 前置条件 | 知识库中已有处理完成的规范文档 |
| 操作流程 | 1. 用户输入问题 2. 点击发送/回车 3. 显示加载状态 4. 展示答案及引用 |
| 交互细节 | 输入框支持多行文本；答案区显示引用块（可点击展开原文）；支持新对话/清空历史 |
| 验收标准 | 输入问题后2-3秒内返回答案；答案包含参数值+出处；引用可点击查看原文 |

#### F2：知识库管理页面 (P0)

| 属性 | 内容 |
|------|------|
| 功能描述 | 上传、查看、管理工程规范文档 |
| 前置条件 | 管理员权限 |
| 操作流程 | 1. 进入知识库页面 2. 上传文档(支持拖拽) 3. 查看处理状态 4. 可删除/重新处理文档 |
| 交互细节 | 文档列表显示文件名、大小、处理状态、语义块数量、上传时间；支持批量上传 |
| 验收标准 | 文档上传后自动触发处理流程；处理完成后知识库可被检索 |

#### F3：文档处理引擎 (P0)

| 属性 | 内容 |
|------|------|
| 功能描述 | 对上传的规范文档进行清洗、语义分块、向量化 |
| 前置条件 | 文档已上传 |
| 处理步骤 | 1. 格式识别(Word/PDF/TXT) 2. 文本提取 3. 清洗(去页眉页脚/统一编码) 4. 表格转自然语言 5. 语义分块 6. Embedding向量化 7. 存入向量数据库 |
| 验收标准 | Word/PDF/TXT均可正确解析；表格被转为可读文本；分块保留章节完整性 |

#### F4：RAG检索引擎 (P0)

| 属性 | 内容 |
|------|------|
| 功能描述 | 接收用户问题，检索相关规范片段，供LLM生成答案 |
| 核心策略 | 向量语义检索(主) + 关键词检索(辅)；top_k=8；chunk_size=384 |
| 验收标准 | 检索结果与问题语义相关；跨章节依赖的信息可被召回 |

#### F5：双层Prompt策略引擎 (P0)

| 属性 | 内容 |
|------|------|
| 功能描述 | 引导LLM按固定推理步骤生成准确、格式统一的答案 |
| 第一层：CoT | 步骤①识别问题类型 → 步骤②定位相关章节 → 步骤③抽取参数 |
| 第二层：Few-shot | 12类参数模板（材料强度、几何尺寸、荷载组合、安全系数等），每类1-2个示例 |
| 正则校验 | 过滤模糊表述（"约等于"）、校验JSON格式、拦截非数字+单位组合 |
| 降级策略 | 校验失败时返回原文片段，不输出不可靠答案 |
| 验收标准 | 输出格式100%符合规范；幻觉输出被正则拦截 |

#### F6：评测面板 (P1，展示亮点)

| 属性 | 内容 |
|------|------|
| 功能描述 | 展示系统效果指标，支撑"数据驱动"叙事 |
| 展示内容 | 评测集概况(220条)；准确率(95%)；端到端响应时间(2.7s)；漏检/截断改善数据 |
| 验收标准 | 页面展示清晰的数据图表和改善对比 |

### 3.2 后续迭代功能 (P2，不在MVP范围)

- 多轮对话能力（上下文记忆）
- 图纸OCR识别
- 规范版本变更追踪
- 用户反馈收集与主动学习
- 移动端适配
- 多租户/多项目空间

### 3.3 明确不做 (Out of Scope)

- 不连接任何公网API服务
- 不做在线协同编辑
- 不做规范原文的自动更新/爬取
- 不做用户权限管理体系（MVP用简易模式）

---

## 四、界面交互要求

### 4.1 整体风格
- 专业工程技术风格，简洁实用
- 蓝灰色调为主，符合工程设计工具视觉
- 布局清晰，信息层级明确

### 4.2 页面结构

**问答主页（核心页面）**
```
┌──────────────────────────────────────────┐
│  Header: Logo + 标题 + 导航(问答/知识库/评测)  │
├──────────────────────────────────────────┤
│                                          │
│         欢迎语 / 快捷问题示例              │
│                                          │
│  ┌────────────────────────────────────┐  │
│  │  问答历史（对话气泡流式展示）        │  │
│  │  - 用户问题                         │  │
│  │  - AI答案 + 引用块                  │  │
│  └────────────────────────────────────┘  │
│                                          │
│  ┌────────────────────────────────┬────┐ │
│  │  输入框（多行文本）              │ 发送│ │
│  └────────────────────────────────┴────┘ │
└──────────────────────────────────────────┘
```

**知识库管理页**
```
┌──────────────────────────────────────────┐
│  Header: 同上                             │
├──────────────────────────────────────────┤
│  [上传文档] 按钮 + 拖拽区域               │
│                                          │
│  文档列表（表格）:                        │
│  文件名 | 大小 | 状态 | 语义块数 | 时间 | 操作│
│  ─────────────────────────────────────── │
│  桥梁规范.docx | 2.3MB | 已完成 | 156块  │
│  ...                                     │
└──────────────────────────────────────────┘
```

### 4.3 关键交互
- 答案加载时显示骨架屏 / 逐字流式输出
- 引用来源可点击弹窗查看原文上下文
- 错误状态友好提示（非技术性报错）
- Demo模式下可预置示例问题，一键填充

---

## 五、非功能需求

### 5.1 性能要求
| 指标 | 要求 |
|------|------|
| 端到端响应时间 | ≤ 3秒 |
| 文档处理速度 | 单文件 ≤ 30秒 |
| 并发支持 | 单用户场景即可（演示环境） |

### 5.2 兼容性要求
- 浏览器：Chrome / Edge 最新两个版本
- 操作系统：Windows 10+ / macOS

### 5.3 可维护性
- 代码需有清晰的分层架构
- Prompt模板与业务逻辑解耦，可独立配置
- 评测数据独立存储，可重复运行

### 5.4 演示友好性
- 支持Demo模式：预置示例文档和问题，无需手动上传即可演示
- 首次启动自动初始化示例数据
- 一键重置演示数据

---

## 六、验收确认

- [x] 无模糊不清的需求描述，所有功能都有明确的验收标准
- [x] 清晰划定了功能边界：MVP 6项核心功能，后续迭代项，明确不做项
- [x] 文档结构化，AI 可直接读懂并执行
